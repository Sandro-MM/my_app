import React from 'react';
import {NextIcon} from "../styles/styles";

const A2BNextIcon = ({ onPress }) => {
    return (
        <NextIcon
            icon="chevron-right-circle"
            iconColor='#FF5A5F'
            size={50}
            onPress={onPress}
        />
    );
};

export default A2BNextIcon;
